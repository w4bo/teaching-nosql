name: "build"
on: [push, pull_request]
jobs:
  launch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: main
      - name: Build Images
        run: |
          ./start.sh